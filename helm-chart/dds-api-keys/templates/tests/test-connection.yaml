apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dds-api-keys-chart.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "dds-api-keys-chart.name" . }}
    helm.sh/chart: {{ include "dds-api-keys-chart.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "dds-api-keys-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
